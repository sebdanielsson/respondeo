{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "semanticCommits": "enabled",
  "minimumReleaseAge": "3 days",
  "description": "config:recommended ignores **/examples/**, but examples/respondeo is the published create-respondeo-app template and must stay dependency-identical to apps/web (enforced by scripts/check-template-sync.sh). Default list minus the examples glob.",
  "ignorePaths": [
    "**/node_modules/**",
    "**/bower_components/**",
    "**/vendor/**",
    "**/__tests__/**",
    "**/test/**",
    "**/tests/**",
    "**/__fixtures__/**"
  ],
  "packageRules": [
    {
      "groupName": "AI SDK",
      "matchPackageNames": ["ai", "@ai-sdk/**"]
    },
    {
      "groupName": "Fumadocs",
      "matchPackageNames": ["fumadocs-**", "@fumadocs/**"]
    }
  ],
  "customManagers": [
    {
      "description": "Update the pnpm version in devEngines.packageManager. Renovate's npm manager only updates the top-level packageManager field, which this repository no longer sets.",
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)package\\.json$/"],
      "matchStrings": [
        "\"packageManager\":\\s*\\{\\s*\"name\":\\s*\"pnpm\",\\s*\"version\":\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "depNameTemplate": "pnpm",
      "datasourceTemplate": "npm"
    }
  ]
}
